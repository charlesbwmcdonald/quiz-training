import { redirect } from "next/navigation";
import { createSupabaseServerClient } from "@/lib/supabase/server";
export default async function AcceptPage({params}:{params:Promise<{token:string}>}){const {token}=await params;const supabase=await createSupabaseServerClient();const {data:user}=await supabase.auth.getUser();if(!user.user)redirect(`/login?next=${encodeURIComponent(`/invite/${token}/accept`)}`);const {error}=await supabase.rpc("accept_training_invitation",{invitation_token:token});if(error)redirect(`/invite/${token}?error=${encodeURIComponent(error.message)}`);const {data:destination}=await supabase.rpc("get_post_login_destination");redirect(typeof destination==="string"?destination:"/app");}
