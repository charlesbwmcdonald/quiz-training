"use server";
import {redirect} from "next/navigation";import {createSupabaseServerClient} from "@/lib/supabase/server";
export async function updatePassword(formData:FormData){const password=String(formData.get("password")??"");const confirm=String(formData.get("confirm")??"");const brand=String(formData.get("brand")??"");if(password.length<8||password!==confirm)redirect(`/reset-password?error=${encodeURIComponent(password.length<8?"Use at least 8 characters.":"Passwords do not match.")}${brand?`&brand=${brand}`:""}`);const supabase=await createSupabaseServerClient();const{error}=await supabase.auth.updateUser({password});if(error)redirect(`/reset-password?error=${encodeURIComponent(error.message)}${brand?`&brand=${brand}`:""}`);await supabase.auth.signOut();redirect(`/login?reset=1${brand?`&brand=${brand}`:""}`)}
