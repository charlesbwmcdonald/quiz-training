import Image from "next/image";import {redirect} from "next/navigation";import {getPublicBrand,getPublicLandingExperience} from "@/lib/branding";import {createSupabaseServerClient} from "@/lib/supabase/server";import {updatePassword} from "./actions";
export default async function ResetPassword({searchParams}:{searchParams:Promise<{brand?:string;error?:string}>}){const q=await searchParams;const supabase=await createSupabaseServerClient();const[{data},brand,experience]=await Promise.all([supabase.auth.getUser(),getPublicBrand(q.brand),getPublicLandingExperience(q.brand)]);if(!data.user)redirect(`/forgot-password?error=Reset+link+expired${q.brand?`&brand=${q.brand}`:""}`);const primary=brand?.primary_color||"#ff4f1f";const secondary=brand?.secondary_color||"#101010";const login=experience?.settings.login;return <main className="grid min-h-screen bg-white lg:grid-cols-2"><section className="relative hidden overflow-hidden bg-cover bg-center p-12 text-white lg:flex lg:flex-col lg:justify-between" style={{backgroundColor:secondary,backgroundImage:login?.image_url?`linear-gradient(rgba(0,0,0,.62),rgba(0,0,0,.72)),url(${login.image_url})`:undefined}}>{brand?.logo_url?<Image src={brand.logo_url} alt={`${brand.name} logo`} width={210} height={48} className="relative z-10 max-h-12 w-auto object-contain brightness-0 invert" priority unoptimized/>:<b className="relative z-10 text-2xl font-black uppercase">JobberTrain</b>}<div className="relative z-10 max-w-xl"><p className="text-sm font-extrabold uppercase italic tracking-[.2em]" style={{color:primary}}>{brand?`${brand.name} learning center`:"Secure account access"}</p><h1 className="mt-4 text-5xl font-extrabold uppercase leading-[.95]">Return to your<br/><span className="italic" style={{color:primary}}>product training.</span></h1><p className="mt-6 max-w-lg text-lg leading-8 text-white/70">Choose a new password to securely continue your academy experience.</p></div><p className="relative z-10 text-sm font-bold uppercase tracking-[.15em] text-white/40">{brand?.name||"Powered by JobberTrain"}</p></section><section className="flex items-center justify-center px-5 py-12 sm:px-10"><div className="w-full max-w-md"><p className="text-sm font-extrabold uppercase italic tracking-[.2em]" style={{color:primary}}>Secure account update</p><h2 className="mt-2 text-4xl font-extrabold uppercase tracking-tight">Choose a new password</h2><p className="mt-3 text-black/60">Your new password must contain at least eight characters.</p>{q.error&&<div className="mt-6 border-l-4 bg-red-50 p-4 font-semibold text-red-900" style={{borderColor:primary}}>{q.error}</div>}<form action={updatePassword} className="mt-8 grid gap-5"><input type="hidden" name="brand" value={brand?.slug||""}/><label className="grid gap-2 font-bold">New password<input name="password" type="password" minLength={8} required autoComplete="new-password" className="min-h-13 border border-black/20 px-4 font-normal outline-none focus:border-black"/></label><label className="grid gap-2 font-bold">Confirm password<input name="confirm" type="password" minLength={8} required autoComplete="new-password" className="min-h-13 border border-black/20 px-4 font-normal outline-none focus:border-black"/></label><button className="min-h-13 font-extrabold uppercase text-white transition hover:brightness-90" style={{backgroundColor:primary}}>Update password</button></form></div></section></main>}
