"use client";import {useState} from "react";import {saveProduct} from "./actions";type Spec={id:string;label:string;value:string};type Initial={id?:string;name?:string;slug?:string;category_name?:string;model_sku?:string;tagline?:string;description?:string;images?:{url:string}[];features?:string[];specs?:{label:string;value:string}[];compatibility?:string;videos?:{url:string}[];downloads?:{url:string}[];product_url?:string;status?:string};const uid=()=>Math.random().toString(36).slice(2);
export default function ProductEditor({primary,initial}:{primary:string;initial?:Initial}){const [name,setName]=useState(initial?.name??"");const [slug,setSlug]=useState(initial?.slug??"");const [specs,setSpecs]=useState<Spec[]>(initial?.specs?.length?initial.specs.map(s=>({...s,id:uid()})):[{id:uid(),label:"",value:""}]);return <form action={saveProduct} className="mt-8 grid gap-6"><input type="hidden" name="productId" value={initial?.id??""}/><input type="hidden" name="specs" value={JSON.stringify(specs.filter(s=>s.label||s.value).map(({label,value})=>({label,value})))}/><section className="border border-black/10 bg-white p-6 sm:p-8"><h2 className="text-xl font-extrabold uppercase">Product identity</h2><div className="mt-5 grid gap-5 sm:grid-cols-2"><label className="grid gap-2 font-bold">Product name<input name="name" value={name} onChange={e=>{setName(e.target.value);if(!initial?.id)setSlug(e.target.value.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,""))}} required className="min-h-12 border px-4 font-normal"/></label><label className="grid gap-2 font-bold">URL slug<input name="slug" value={slug} onChange={e=>setSlug(e.target.value)} required className="min-h-12 border px-4 font-normal"/></label><label className="grid gap-2 font-bold">Category<input name="category" defaultValue={initial?.category_name} placeholder="Bumper Towing" className="min-h-12 border px-4 font-normal"/></label><label className="grid gap-2 font-bold">Model / SKU<input name="modelSku" defaultValue={initial?.model_sku} className="min-h-12 border px-4 font-normal"/></label></div><label className="mt-5 grid gap-2 font-bold">Positioning statement<input name="tagline" defaultValue={initial?.tagline} className="min-h-12 border px-4 font-normal"/></label><label className="mt-5 grid gap-2 font-bold">Product overview<textarea name="description" defaultValue={initial?.description} rows={6} className="border p-4 font-normal"/></label></section><section className="border border-black/10 bg-white p-6 sm:p-8"><h2 className="text-xl font-extrabold uppercase">Sales knowledge</h2><label className="mt-5 grid gap-2 font-bold">Image URLs <span className="text-xs font-normal text-black/45">One per line</span><textarea name="images" defaultValue={initial?.images?.map(x=>x.url).join("\n")} rows={4} className="border p-4 font-normal"/></label><label className="mt-5 grid gap-2 font-bold">Features & selling points <span className="text-xs font-normal text-black/45">One per line</span><textarea name="features" defaultValue={initial?.features?.join("\n")} rows={5} className="border p-4 font-normal"/></label><fieldset className="mt-5"><legend className="font-bold">Specifications</legend><div className="mt-2 grid gap-2">{specs.map(s=><div key={s.id} className="grid grid-cols-[1fr_1fr_auto] gap-2"><input value={s.label} onChange={e=>setSpecs(all=>all.map(x=>x.id===s.id?{...x,label:e.target.value}:x))} placeholder="Capacity" className="min-h-11 border px-3"/><input value={s.value} onChange={e=>setSpecs(all=>all.map(x=>x.id===s.id?{...x,value:e.target.value}:x))} placeholder="21,000 lb" className="min-h-11 border px-3"/><button type="button" onClick={()=>setSpecs(all=>all.filter(x=>x.id!==s.id))} className="px-3 font-bold">×</button></div>)}</div><button type="button" onClick={()=>setSpecs(all=>[...all,{id:uid(),label:"",value:""}])} className="mt-3 text-sm font-extrabold uppercase" style={{color:primary}}>+ Add specification</button></fieldset><label className="mt-5 grid gap-2 font-bold">Compatibility & fitment<textarea name="compatibility" defaultValue={initial?.compatibility} rows={4} className="border p-4 font-normal"/></label></section><section className="border border-black/10 bg-white p-6 sm:p-8"><h2 className="text-xl font-extrabold uppercase">Media & resources</h2><div className="mt-5 grid gap-5"><label className="grid gap-2 font-bold">Video URLs <span className="text-xs font-normal text-black/45">One per line</span><textarea name="videos" defaultValue={initial?.videos?.map(x=>x.url).join("\n")} rows={3} className="border p-4 font-normal"/></label><label className="grid gap-2 font-bold">Manual / download URLs <span className="text-xs font-normal text-black/45">One per line</span><textarea name="downloads" defaultValue={initial?.downloads?.map(x=>x.url).join("\n")} rows={3} className="border p-4 font-normal"/></label><label className="grid gap-2 font-bold">Official product URL<input name="productUrl" type="url" defaultValue={initial?.product_url} className="min-h-12 border px-4 font-normal"/></label></div></section><div className="flex justify-end gap-3"><button name="intent" value="draft" className="min-h-12 border-2 border-black px-6 font-bold uppercase">Save draft</button><button name="intent" value="published" className="min-h-12 px-6 font-extrabold uppercase text-white" style={{backgroundColor:primary}}>Save & publish</button></div></form>}
