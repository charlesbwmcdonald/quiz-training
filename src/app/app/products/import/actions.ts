"use server";import {redirect} from "next/navigation";import {createSupabaseServerClient} from "@/lib/supabase/server";type Row=Record<string,string>;const parts=(v:string)=>String(v||"").split("|").map(x=>x.trim()).filter(Boolean);export async function importProducts(formData:FormData){let rows:Row[];try{rows=JSON.parse(String(formData.get("rows")??"[]"))}catch{redirect("/app/products/import?error=The+CSV+data+could+not+be+read.")}const supabase=await createSupabaseServerClient();let count=0;for(const row of rows!){if(!row.name||!row.slug)continue;const specs=parts(row.specs).map(item=>{const i=item.indexOf("=");return{label:i<0?item:item.slice(0,i).trim(),value:i<0?"":item.slice(i+1).trim()}});const {error}=await supabase.rpc("save_manufacturer_product",{target_id:null,product_name:row.name.trim(),product_slug:row.slug.trim().toLowerCase(),category_name:row.category||"",model_sku:row.model_sku||"",tagline:row.tagline||"",description:row.description||"",images:parts(row.image_urls).map(url=>({url})),features:parts(row.features),specs,compatibility:row.compatibility||"",videos:parts(row.video_urls).map(url=>({url})),downloads:parts(row.download_urls).map(url=>({url})),product_url:row.product_url||"",product_status:"draft"});if(error)redirect(`/app/products/import?error=${encodeURIComponent(`${row.name}: ${error.message}`)}`);count++}redirect(`/app/products?imported=${count}`)}
