import {redirect} from "next/navigation";import {ManufacturerHeader} from "@/components/manufacturer-shell";import {getActiveBrand} from "@/lib/branding";import {createSupabaseServerClient} from "@/lib/supabase/server";import {AssignmentRow,ReportWorkspace} from "./report-workspace";
type Summary={retailers:number;retailer_learners:number;team_learners:number;assigned:number;completed:number;overdue:number;completion_rate:number;pass_rate:number};type Report={summary:Summary;assignments:AssignmentRow[]};
function Card({label,value,detail}:{label:string;value:string|number;detail:string}){return <article className="border border-black/10 bg-white p-5 shadow-sm"><span className="text-xs font-extrabold uppercase tracking-wide text-black/45">{label}</span><b className="mt-2 block text-4xl">{value}</b><p className="mt-1 text-sm text-black/50">{detail}</p></article>}
export default async function ReportsPage(){const supabase=await createSupabaseServerClient();const[{data:auth},brand]=await Promise.all([supabase.auth.getUser(),getActiveBrand()]);if(!auth.user)redirect("/login");if(!brand?.can_view_reports)redirect("/app");const{data,error}=await supabase.rpc("manufacturer_training_report");const report=data as Report|null;const s=report?.summary??{retailers:0,retailer_learners:0,team_learners:0,assigned:0,completed:0,overdue:0,completion_rate:0,pass_rate:0};return <div className="min-h-screen bg-[#f4f4f2]"><ManufacturerHeader brand={brand} email={auth.user.email}/><main className="mx-auto max-w-7xl px-5 py-10 lg:px-8 lg:py-14"><p className="text-sm font-extrabold uppercase italic tracking-[.2em]" style={{color:brand.primary_color}}>Network performance</p><h1 className="mt-2 text-4xl font-extrabold uppercase tracking-tight sm:text-5xl">Training reports</h1><p className="mt-3 max-w-2xl text-black/60">Track quizzes and courses across retailer learners and your manufacturer team.</p>{error&&<div className="mt-7 border-l-4 bg-red-50 p-4 font-semibold text-red-900">Reports are temporarily unavailable.</div>}<section className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><Card label="Learners" value={s.retailer_learners+s.team_learners} detail={`${s.retailer_learners} retailer - ${s.team_learners} team`}/><Card label="Completion rate" value={`${s.completion_rate}%`} detail={`${s.completed} of ${s.assigned} assignments`}/><Card label="Quiz pass rate" value={`${s.pass_rate}%`} detail="Latest completed quiz attempts"/><article className="p-5 text-white shadow-sm" style={{backgroundColor:brand.secondary_color}}><span className="text-xs font-extrabold uppercase tracking-wide text-white/60">Needs attention</span><b className="mt-2 block text-4xl">{s.overdue}</b><p className="mt-1 text-sm text-white/65">Overdue assignments</p></article></section><ReportWorkspace rows={report?.assignments??[]} primary={brand.primary_color}/></main></div>}
