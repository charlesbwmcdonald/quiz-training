"use server";import {redirect} from "next/navigation";import {createSupabaseServerClient} from "@/lib/supabase/server";export async function assignCourse(formData:FormData){const supabase=await createSupabaseServerClient();const {error}=await supabase.rpc("assign_manufacturer_course",{target_company_id:String(formData.get("companyId")),target_course_id:String(formData.get("courseId")),required:true,target_due_at:null});if(error)redirect(`/app/courses?error=${encodeURIComponent(error.message)}`);redirect("/app/courses?assigned=1");}
export async function assignInternalCourse(formData:FormData){const supabase=await createSupabaseServerClient();const {error}=await supabase.rpc("assign_course_to_internal_team",{target_course_id:String(formData.get("courseId")),required:true,target_due_at:null});if(error)redirect(`/app/courses?error=${encodeURIComponent(error.message)}`);redirect("/app/courses?internal=1");}
export async function duplicateCourse(formData:FormData){const supabase=await createSupabaseServerClient();const {error}=await supabase.rpc("duplicate_manufacturer_course",{target_course_id:String(formData.get("courseId"))});if(error)redirect(`/app/courses?error=${encodeURIComponent(error.message)}`);redirect("/app/courses?duplicated=1");}
export async function removeCourse(formData:FormData){const supabase=await createSupabaseServerClient();const {data,error}=await supabase.rpc("remove_manufacturer_course",{target_course_id:String(formData.get("courseId"))});if(error)redirect(`/app/courses?error=${encodeURIComponent(error.message)}`);redirect(`/app/courses?${data==="deleted"?"deleted":"archived"}=1`);}
