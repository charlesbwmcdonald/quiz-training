"use server";import {revalidatePath} from "next/cache";import {createSupabaseServerClient} from "@/lib/supabase/server";export async function completeInternalBlock(formData:FormData){const courseId=String(formData.get("courseId"));const supabase=await createSupabaseServerClient();await supabase.rpc("complete_internal_course_block",{target_course_id:courseId,target_block_id:String(formData.get("blockId"))});revalidatePath(`/app/courses/${courseId}/internal`);}
